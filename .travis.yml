language: python
dist: xenial

#cache:
#  - directories:
#    - $HOME/miniconda

python:
  - "3.7"

install:
  # Install Anaconda
  - wget https://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh -O /tmp/miniconda.sh
  - bash /tmp/miniconda.sh -b -p $HOME/miniconda
  - export PATH="$HOME/miniconda/bin:$PATH"
  - hash -r
  - conda config --set always_yes yes --set changeps1 no
  - conda update -q conda

  # install dependencies
  - source $HOME/miniconda/etc/profile.d/conda.sh
  - travis_wait 20 conda create -q -n drop -c conda-forge -c bioconda drop
  - conda activate drop

  # install drop
  - pip uninstall wbuild -y
  - pip install -e . -vv

script: 
  - conda list 
  - java -version
  - gatk --help
  - samtools --version
  - bcftools --version
  - drop --version
  - python --version
  
  - mkdir drop_demo
  - cd drop_demo
  - drop demo
  - snakemake -n
  #- snakemake aberrantExpression --jobs 2 --cores 2 --latency-wait 30
  #- snakemake aberrantSplicing --jobs 2 --cores 2 --latency-wait 30
  #- snakemake mae --jobs 2 --cores 2 --latency-wait 30
  - snakemake rnaVariantCalling --jobs 2 --cores 2 --latency-wait 30
  #- snakemake --jobs 2 --cores 2
  #- snakemake exportCounts --cores 2
